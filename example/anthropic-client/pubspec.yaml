name: anthropic_client
description: An Anthropic Messages API client example backed by MCP tools.
version: 1.0.1
publish_to: none

environment:
  sdk: ^3.9.0

# Add regular dependencies here.
dependencies:
  anthropic_sdk_dart: ^6.0.0
  mcp_dart:
    path: ../../

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0
