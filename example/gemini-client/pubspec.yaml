name: gemini_client
description: A Gemini Interactions API client that connects to MCP servers.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.2

dependencies:
  http: ^1.4.0
  mcp_dart:
    path: ../..

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
