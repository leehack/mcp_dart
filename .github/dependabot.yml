version: 2

updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      github-actions:
        patterns:
          - '*'

  # Keep non-Actions updates ungrouped until
  # https://github.com/dependabot/dependabot-core/issues/14780 is fixed.
  # Dynamic group-by PRs cannot currently be rebased or recreated.
  - package-ecosystem: pub
    directories:
      - /
      - /packages/mcp_dart_cli
      - /packages/mcp_dart_cli/test/fixtures/dart_mcp_project
      - /example/anthropic-client
      - /example/fetch-server
      - /example/flutter_http_client
      - /example/gemini-client
      - /example/jaspr-client
    schedule:
      interval: weekly

  - package-ecosystem: npm
    directories:
      - /test/interop/ts
      - /test/interop/ts_2026_07_28
    schedule:
      interval: weekly
    ignore:
      # Keep compile-time Node APIs aligned with the Node 24 CI baseline.
      - dependency-name: '@types/node'
        update-types:
          - version-update:semver-major
      # Remove once typescript-eslint officially supports TypeScript 7.
      - dependency-name: typescript
        update-types:
          - version-update:semver-major

  - package-ecosystem: pip
    directory: /test/interop/python
    schedule:
      interval: weekly
    ignore:
      # This fixture intentionally exercises Python SDK 1.x interoperability.
      # Python SDK 2.x is covered by /test/interop/python_2026_07_28. Remove this
      # exception when the legacy interoperability lane is retired.
      - dependency-name: mcp
        update-types:
          - version-update:semver-major

  - package-ecosystem: pip
    directory: /test/interop/python_2026_07_28
    schedule:
      interval: weekly
