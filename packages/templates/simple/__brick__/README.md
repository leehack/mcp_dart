# {{name}}

A Model Context Protocol (MCP) server created with `mcp_dart`.

## Running the Server

```bash
dart run bin/server.dart
```

## Adding Tools

Edit `bin/server.dart` to add more tools and capabilities.
